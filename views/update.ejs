<!DOCTYPE html>
<html lang="en">
<head>
    <title>TODOList</title>
    <style>
        html {
            background-color: #e4e9fd;
            background-image: -webkit-linear-gradient(65deg, #f77e21 60%, #fac213 50%);
            min-height: 1000px;
            font-family: "helvetica neue";
        }
        .box {
            max-width: 530px;
            margin: 25px auto;
            background: #fffbc1;
            border-radius: 5px;
            box-shadow: 5px 5px 15px -5px rgba(0, 0, 0, 0.3);
            padding: 5px;
        }
        form {
            text-align: center;
            margin-left: 20px;
        }
        button {
            min-height: 70px;
            width:70px;
            border-radius: 50%;
            border-color: transparent;
            background-color: #cf0a0a;
            color: #fff;
            font-size: 35px;
            font-weight: bolder;
            padding-bottom: 6px;
            border-width: 0;
            cursor: pointer;
        }
        input[type="text"] {
            text-align: center;
            height: 60px;
            top: 10px;
            border: none;
            background: transparent;
            font-size: 20px;
            font-weight: 200;
            width: 313px;
        }

        input[type="text"]:focus {
            outline: none;
            box-shadow: inset 0 -3px 0 0 #f57328;
            border-radius: 2px;
        }

        ::placeholder {
            color: black;
            opacity: 1;
        }   
    </style>
</head>
<body>
        <div class="box">
            <form action="/update/<%=flag %>" method="post">
                <input type="text" name="changedwork" placeholder="Update item">
                <button name="changebtn" type="submit" value="change">☑️</button>
            </form>
        </div>
</div>
</body>
</html>
